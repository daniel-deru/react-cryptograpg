import styled from "styled-components"

export const HelpPage = styled.main`
    color: ${({theme}) => theme.colors.green};
    p {
        width: 70%;
        margin: auto;
        font-size: 1.25em;
    }
    section {
        ul {
            li {
                text-align: left;
            }
        
        }
    }

`